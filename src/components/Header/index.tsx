import { StyledLink as Link } from '../Link/styles';
import { Nav } from './styles';

const Header = () => {
  return (
    <Nav>
      <Link to="/">TV Series Search</Link>
    </Nav>
  );
};

export default Header;
